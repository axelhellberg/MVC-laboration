<h2 class="clock">
    Idag är det
    @System.Globalization.CultureInfo.GetCultureInfo("sv-SE").DateTimeFormat.GetDayName(@DateTime.Now.DayOfWeek).
    Klockan är
    @DateTime.Now.ToShortTimeString().
    <span>(Senast uppdaterad @DateTime.Now)</span>
</h2>
